from setuptools import setup, find_packages

setup(
	name="asyncml-broker",
	version="0.1.0",
	description="A Python client for high-performance AsyncML C++ Broker.",
	packages=find_packages(),
	install_requires=[]
)